pipeline {
    agent any
    stages{
        stage('checkout'){
            steps{
                // deleteDir()
                // git 'https://github.com/bharath0080/simple-java-maven-app.git'
            }
        }
        stage('build'){
            steps{
                sh 'mvn clean verify’
              }
            }
        }
    }
}